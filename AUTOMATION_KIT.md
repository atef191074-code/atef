# حزمة الأتمتة الجاهزة (Make / Zapier) — جاهزة للنسخ المباشر

نظام: **صياغة الأهداف السلوكية وتفعيلها في تنفيذ خطة الدرس اليومية**

---

## 1) System Prompt للتصحيح الآلي عبر Claude
> يُلصق في وحدة "Anthropic Claude" داخل Make أو في خطوة "Run a Prompt" بـ Zapier. يستقبل إجابات المعلّم ومفتاح الحل ويُرجِع وسماً واحداً فقط.

```
أنت مُصحِّح آلي صارم ومحايد لمنصة تدريب المعلّمين. مهمتك تصحيح اختبار موضوعي وإصدار وسم نهائي.

القواعد:
1. ستتلقى كائن JSON يحتوي: answers (إجابات المعلّم كأرقام خيارات تبدأ من 0)، key (مفتاح الحل الصحيح)، test_type (pre / unit1 / unit2 / post).
2. احسب عدد الإجابات الصحيحة بمطابقة كل عنصر في answers مع نظيره في key بالترتيب.
3. احسب النسبة المئوية = (الصحيحة ÷ العدد الكلي) × 100 وقرّبها لأقرب عدد صحيح.
4. شرط النجاح:
   - إذا كان test_type = "pre": دائماً [PASSED] (قياسي بدون شرط رسوب) واذكر النسبة فقط للأرشفة.
   - لبقية الاختبارات (unit1, unit2, post): [PASSED] إذا كانت النسبة 80% فأكثر، وإلا [FAILED].
5. أعِد ردّك بصيغة JSON فقط دون أي شرح أو نص إضافي، بالشكل التالي بالضبط:

{
  "test_type": "<النوع>",
  "correct": <عدد الإجابات الصحيحة>,
  "total": <العدد الكلي>,
  "percentage": <النسبة>,
  "tag": "[PASSED]" أو "[FAILED]",
  "action": "ISSUE_CERTIFICATE" إذا كان post و PASSED، أو "PROCEED" إذا وحدة PASSED، أو "REVIEW" إذا FAILED
}

لا تُضِف نصاً قبل JSON أو بعده. لا تستخدم Markdown. أعِد الكائن خاماً.
```

**مثال على المُدخل (Input JSON):**
```json
{
  "test_type": "post",
  "answers": [1,2,1,0,1,0,1,1,0,1],
  "key":     [1,2,1,0,1,0,1,1,0,1]
}
```

**المُخرج المتوقّع من Claude:**
```json
{"test_type":"post","correct":10,"total":10,"percentage":100,"tag":"[PASSED]","action":"ISSUE_CERTIFICATE"}
```

---

## 2) قالب البريد الإلكتروني المؤتمت (يُرسَل عند [PASSED] في الاختبار البعدي)
> الوجهة: **النائب الأكاديمي + المنسّق** (CC). المرفق: نسخة PDF من الشهادة. يدعم متغيرات Make/Zapier `{{...}}`.

**To:** `{{vice_academic_email}}`
**Cc:** `{{coordinator_email}}`
**Subject:** ‏[اجتياز برنامج تدريبي] — {{teacher_name}} | قسم {{department}} | {{final_percentage}}%

**Body (HTML أو نص):**
```
السلام عليكم ورحمة الله وبركاته،

نفيدكم بأن المعلّم/ـة: {{teacher_name}}
من قسم: {{department}}
قد اجتاز/ت بنجاح البرنامج التدريبي:
«صياغة الأهداف السلوكية وتفعيلها في تنفيذ خطة الدرس اليومية»

ملخص النتائج:
• الاختبار القبلي (مرجعي): {{score_pre}}%
• اختبار الوحدة الأولى: {{score_unit1}}%
• اختبار الوحدة الثانية: {{score_unit2}}%
• الاختبار البعدي الشامل: {{final_percentage}}%
• الحالة النهائية: [PASSED] — مستوفٍ لشرط الإتقان (80% فأكثر)

مرفق طيّه نسخة PDF من شهادة الاجتياز للاعتماد والأرشفة.

تاريخ الإصدار: {{issue_date}}

مع التحية،
إدارة التطوير والتدريب
(رسالة مؤتمتة — لا حاجة للرد)
```
> **مرفق:** `Certificate_{{teacher_name}}.pdf`

---

## 3) نص قالب شهادة الاجتياز (بالمتغيرات الذكية)
> يُستخدم في مولّد PDF (مثل APITemplate.io / PDFMonkey / Google Docs → PDF داخل Make). المتغيرات تُستبدل آلياً.

```
╔══════════════════════════════════════════════╗

         مدرسة ابن خلدون الإعدادية للبنين
                  ✦ شهادة اجتياز ✦
        (تصدر عن إدارة المدرسة — معتمدة رقمياً)

تشهد إدارة مدرسة ابن خلدون الإعدادية للبنين
بأنّ المعلّم/ـة:

              【 {{الاسم}} 】

من قسم  «{{القسم}}»

قد أتمّ بنجاح واجتاز البرنامج التدريبي:
«صياغة الأهداف السلوكية وتفعيلها
   في تنفيذ خطة الدرس اليومية»

محققاً نسبة إتقان قدرها: {{النسبة}}
ومستوفياً شرط الاجتياز (80% فأكثر).

       التاريخ: {{issue_date}}     الحالة: [PASSED]

ـــــــــــــــ      ـــــــــــــــ      ـــــــــــــــ
 منسّق التطوير      النائب الأكاديمي      مدير المدرسة
    المهني

╚══════════════════════════════════════════════╝
```

**جدول ربط المتغيرات (Variable Mapping) — للنسخ في بوابة الأتمتة:**

| متغيّر الشهادة | حقل الـ Payload القادم من النموذج |
|---|---|
| `{{الاسم}}` | `teacher_name` |
| `{{القسم}}` | `department` |
| `{{النسبة}}` | `final_percentage` + "%" |
| `{{المدرسة}}` | `school` |
| `{{issue_date}}` | `issue_date` |

> **التواقيع الثلاثة** (منسّق التطوير المهني / النائب الأكاديمي / مدير المدرسة) ثابتة في تذييل قالب الـ PDF. أضِف صور التواقيع أو الأختام فوق كل خط في مولّد الـ PDF إن رغبت.

---

## 4) مخطط سير العمل الكامل (Workflow)
```
[نموذج التسجيل + الاختبارات في الويب]
        │  (يُرسل Webhook بحمولة JSON)
        ▼
[Make/Zapier: استقبال Webhook]
        │
        ▼
[Claude — System Prompt للتصحيح] ──► يُرجِع {tag, percentage, action}
        │
        ├─ action = REVIEW  ──► إيقاف + إشعار المعلّم بالمراجعة
        │
        └─ action = ISSUE_CERTIFICATE (post + PASSED)
                 │
                 ├─► [مولّد PDF] يملأ قالب الشهادة بالمتغيرات
                 │
                 └─► [إرسال البريد المؤتمت] إلى النائب الأكاديمي + المنسّق
                          مع مرفق PDF
```

**حمولة الـ Webhook المُرسلة من البوابة (مطابقة لمخرجات التطبيق):**
```json
{
  "status": "PASSED",
  "program": "صياغة الأهداف السلوكية وتفعيلها في تنفيذ خطة الدرس اليومية",
  "teacher_name": "...",
  "department": "...",
  "teacher_email": "...",
  "coordinator_email": "...",
  "vice_academic_email": "...",
  "scores": { "pre": 60, "unit1": 80, "unit2": 100, "post_test": 90 },
  "final_percentage": 90,
  "issue_date": "...",
  "certificate_variables": {
    "{{الاسم}}": "...", "{{القسم}}": "...", "{{النسبة}}": "90%"
  }
}
```
